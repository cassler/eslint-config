{
  "name": "@cassler/eslint-config",
  "private": true,
  "version": "7.1.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "pub": "lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
